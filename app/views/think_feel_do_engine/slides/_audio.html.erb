<%# referenced by both bit_maker and bit_core %>
<% if (slide.options || {})[:soundcloud_title] %>
  <%= render partial: "think_feel_do_engine/shared/jplayer", locals: { title: slide.options[:soundcloud_title] } %>
<% end %>
