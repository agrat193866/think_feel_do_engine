<%# referenced by both bit_maker and bit_core %>
<% if (slide.options || {})[:audio_url] %>
  <%= render partial: "think_feel_do_engine/shared/jplayer", locals: { slide_id: slide.id, audio_url: slide.options[:audio_url] } %>
<% end %>
