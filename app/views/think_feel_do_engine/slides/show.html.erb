<% if @slideshow %>
  <h1>Slideshow <small><%= @slideshow.title %></small></h1>
<% end %>

<%# referenced by both bit_maker and bit_core %>
<% slide = slide || @slide %>
<%= render "think_feel_do_engine/slides/slide", slide: slide %>

<% if @slideshow %>
  <%= link_to "Done", arm_bit_maker_slideshow_path(@arm,@slideshow), class: "btn btn-primary pull-right" %>
<% end %>
